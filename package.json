{
  "name": "svelte-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "format": "prettier src/ --write --no-semi true bracket-new-line false --svelte-sort-order 'options-scripts-markup-styles'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.2.0",
    "carbon-components-svelte": "^0.74.0",
    "sass": "^1.62.1",
    "svelte": "^3.57.0",
    "svelte-preprocess": "^5.0.3",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "mathjs": "^11.9.1"
  }
}
